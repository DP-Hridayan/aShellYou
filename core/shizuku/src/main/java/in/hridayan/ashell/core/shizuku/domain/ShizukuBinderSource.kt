package `in`.hridayan.ashell.core.shizuku.domain

/**
 * Which Shizuku server delivered the binder the app currently holds.
 */
enum class ShizukuBinderSource {
    NONE,
    STOCK,
    PLUS,
    UNKNOWN
}
