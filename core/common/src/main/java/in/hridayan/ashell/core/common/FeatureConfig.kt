package `in`.hridayan.ashell.core.common

/**
 * Global configuration for app features.
 * Values are initialized by the app module based on build flavors or other configurations.
 */
object FeatureConfig {
    var isAiEnabled: Boolean = true
}
